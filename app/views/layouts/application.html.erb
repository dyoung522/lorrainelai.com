<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Lorraine Lai" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Lorraine Lai">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-warm-white text-text-primary min-h-screen">
    <%# Flash messages %>
    <% if notice.present? %>
      <div class="fixed top-4 left-1/2 -translate-x-1/2 z-50 bg-sage border border-sage text-text-primary px-6 py-3 rounded-lg shadow-sm" role="alert">
        <%= notice %>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="fixed top-4 left-1/2 -translate-x-1/2 z-50 bg-dusty-rose border border-dusty-rose-dark text-text-primary px-6 py-3 rounded-lg shadow-sm" role="alert">
        <%= alert %>
      </div>
    <% end %>

    <main>
      <%= yield %>
    </main>

    <%# Footer %>
    <footer class="py-8 text-center">
      <div class="text-sm text-text-muted">
        <% if signed_in? %>
          <%= button_to "Logout", logout_path, method: :delete, class: "hover:text-dusty-rose-dark transition-colors" %>
        <% else %>
          <%= button_to "Login", "/auth/google_oauth2", method: :post, class: "hover:text-dusty-rose-dark transition-colors", data: { turbo: false } %>
        <% end %>
      </div>
    </footer>
  </body>
</html>
