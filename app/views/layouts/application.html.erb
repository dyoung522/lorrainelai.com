<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Lorraine Lai" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Lorraine Lai">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Cropper.js CSS for image cropping %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/cropperjs@1.6.2/dist/cropper.min.css">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>

    <%# Apply theme immediately to prevent flash of wrong theme %>
    <script>
      (function() {
        const stored = localStorage.getItem('theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        if (stored === 'dark' || (!stored && prefersDark)) {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>
  </head>

  <body class="bg-warm-white text-text-primary min-h-screen">
    <%# Theme toggle in header %>
    <header class="fixed top-4 right-4 z-40" data-controller="theme">
      <button type="button"
              data-action="click->theme#toggle"
              class="p-2 rounded-full bg-white/50 dark:bg-gray-700/50 hover:bg-white/80 dark:hover:bg-gray-600/80 text-text-muted hover:text-text-primary transition-colors shadow-sm"
              title="Toggle dark mode">
        <%# Sun icon - shown in dark mode to switch to light %>
        <svg data-theme-target="sunIcon" class="size-5 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
        </svg>
        <%# Moon icon - shown in light mode to switch to dark %>
        <svg data-theme-target="moonIcon" class="size-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
        </svg>
      </button>
    </header>

    <%# Flash messages with auto-dismiss %>
    <% if notice.present? %>
      <div class="flash-message fixed top-4 left-1/2 -translate-x-1/2 z-50 bg-sage border border-sage text-text-primary px-6 py-3 rounded-lg shadow-sm"
           role="alert"
           data-controller="flash"
           data-action="animationend->flash#remove">
        <%= notice %>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="flash-message fixed top-4 left-1/2 -translate-x-1/2 z-50 bg-dusty-rose border border-dusty-rose-dark text-text-primary px-6 py-3 rounded-lg shadow-sm"
           role="alert"
           data-controller="flash"
           data-action="animationend->flash#remove">
        <%= alert %>
      </div>
    <% end %>

    <main>
      <%= yield %>
    </main>

    <%# Footer %>
    <footer class="py-8 text-center">
      <div class="text-sm text-text-muted">
        <% if signed_in? %>
          <%= button_to "Logout", logout_path, method: :delete, class: "hover:text-dusty-rose-dark transition-colors" %>
        <% else %>
          <%= button_to "Login", "/auth/google_oauth2", method: :post, class: "hover:text-dusty-rose-dark transition-colors", data: { turbo: false } %>
        <% end %>
      </div>
    </footer>
  </body>
</html>
